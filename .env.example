# Copy this file to .env and fill in the values for THIS project's own Supabase project.
# Only PUBLIC values here - VITE_ vars are bundled into the browser JS.
# Never put the service_role key in this project (frontend). It is server-only.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
